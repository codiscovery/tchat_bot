{
  "name": "twitch_bot",
  "version": "0.1.0",
  "description": "Twitch bot",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node src/server.js | pino-pretty",
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "push": "git push && git push personal main && git push heroku main",
    "gp": "git add . && git commit -m '$npm_config_m' && git push origin main"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/static": "^6.4.0",
    "@fastify/websocket": "^6.0.1",
    "dotenv": "^16.0.1",
    "fastify": "^4.1.0",
    "tmi.js": "^1.8.5"
  },
  "devDependencies": {
    "pino-pretty": "^8.0.0"
  }
}
